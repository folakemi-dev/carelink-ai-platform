apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: carelink-dev
  labels:
    app: appointment-service
    environment: development
    project: carelink-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
        environment: development
        project: carelink-ai
    spec:
      containers:
        - name: appointment-service
          image: carelink-appointment-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: carelink-dev
  labels:
    app: appointment-service
    environment: development
    project: carelink-ai
spec:
  type: ClusterIP
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8003