apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: carelink-dev
  labels:
    app: notification-service
    environment: development
    project: carelink-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        environment: development
        project: carelink-ai
    spec:
      containers:
        - name: notification-service
          image: carelink-notification-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8004
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: carelink-dev
  labels:
    app: notification-service
    environment: development
    project: carelink-ai
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8004