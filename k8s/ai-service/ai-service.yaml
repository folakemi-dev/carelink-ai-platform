apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  namespace: carelink-dev
  labels:
    app: ai-service
    environment: development
    project: carelink-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
        environment: development
        project: carelink-ai
    spec:
      containers:
        - name: ai-service
          image: carelink-ai-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8005
          readinessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: carelink-dev
  labels:
    app: ai-service
    environment: development
    project: carelink-ai
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8005