apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: carelink-dev
  labels:
    app: auth-service
    environment: development
    project: carelink-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
        environment: development
        project: carelink-ai
    spec:
      containers:
        - name: auth-service
          image: carelink-auth-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8001
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: carelink-dev
  labels:
    app: auth-service
    environment: development
    project: carelink-ai
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001